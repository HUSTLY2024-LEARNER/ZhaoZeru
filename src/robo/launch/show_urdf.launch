<launch>

    <!-- 设置参数 -->
    <param name="robot_description" textfile="$(find robo)/urdf/mycar.urdf" />

    <!-- 启动 rviz -->
    <node pkg="rviz" type="rviz" name="rviz" args="-d $(find robo)/config/mycar.rviz"/>

</launch>
